apply plugin: "org.jetbrains.dokka"

dokka {
    outputDirectory = "$rootDir/docs/api"
    outputFormat = "gfm"

    multiplatform {
        common {}
        js {}
        jvm {}
        "native" {}
    }
}
